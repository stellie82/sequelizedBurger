<header><h1>Burgers Bucket List</h1></header>

<div class="container">
    <div class="section">
        <h2>Hungry for more</h2>
        <ul>
            {{#each burgers}}
            {{#unless devoured}}
            {{> burgers/burger-block devour=true}}
            {{/unless}}
            {{/each}}
        </ul>
    </div>
    <div class="section">
        <h2>Been there, ate that</h2>
        <ul>
            {{#each burgers}}
            {{#if devoured}}
            <li>{{burger_name}}</li>
            {{/if}}
            {{/each}}
        </ul>
    </div>
</div>

<p><i>Enter the name of a burger that you want to eat and click the "Submit" button.</i></p>

<form class="create-form">
    <div class="form-group">
        <label for="burger">Name of burger</label>
        <input type="text" id="burger" name="burger">
    </div>

    <button type="submit" id="submit">Submit</button>
</form>